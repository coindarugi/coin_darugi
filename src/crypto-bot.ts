// 🚨 긴급 복구 모드
// 사이트 정상화를 위해 봇 로직을 일시적으로 비활성화합니다.
export async function runCryptoBot(env: any) {
  console.log('⚠️ 안정성을 위해 트위터 봇이 일시 정지되었습니다.');
  return { success: true, message: 'Bot disabled for stability' };
}
