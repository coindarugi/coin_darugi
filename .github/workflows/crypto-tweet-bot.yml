name: 🤖 Crypto Tweet Bot

on:
  schedule:
    # 하루 2회 실행 (UTC 기준)
    - cron: '0 0 * * *'   # UTC 00:00 = 한국 오전 9시 (아시아 타임)
    - cron: '0 12 * * *'  # UTC 12:00 = 한국 오후 9시 (미국/유럽 타임)
  
  workflow_dispatch:  # 수동 실행 가능

jobs:
  tweet-bot:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm install
      
      - name: 🤖 Run crypto tweet bot
        env:
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          node crypto-tweet-bot.js
      
      - name: ✅ Success notification
        if: success()
        run: echo "✅ 트윗 발행 성공! 5개 언어로 트윗이 발행되었습니다."
      
      - name: ❌ Failure notification
        if: failure()
        run: echo "❌ 트윗 발행 실패! 로그를 확인하세요."
